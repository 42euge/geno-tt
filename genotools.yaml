name: geno-tt
version: "0.5.0"
description: Terminal/session + workspace manager — the `tt` CLI. Remote tmux sessions, the code-org workspace scheme, and whole-workspace git worktrees. Part of the geno-tools ecosystem.
license: MIT
author: 42euge
